import React from 'react';
import Layout from '../layout';

const Webhook = () => {
    return (
        <Layout>
            <div className="max-w-7xl mx-auto px-4 sm:px-6 md:px-8">
                <h1 className="text-2xl font-semibold text-gray-900">Webhook</h1>
            </div>
        </Layout>
    );
};

export default Webhook;
